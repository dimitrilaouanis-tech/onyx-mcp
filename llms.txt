# Onyx Actions — Paid Agent MCP (x402 USDC on Base + Solana)

> Onyx Actions is the first **MCP meta-router** — a paid Model Context Protocol server that also aggregates the entire x402 ecosystem. AI agents pay per call in USDC; no API keys, no signup. 67 tools across Base + Solana on-chain primitives, browser automation, MCP/x402 ecosystem ops, research intel — PLUS `onyx_mcp_router` which routes capability queries across the full 1000-route paid-MCP corpus and returns the best-price best-fit endpoint.

## Endpoints

- Remote MCP (HTTP, recommended): https://onyx-actions.onrender.com/mcp/
- Local stdio: `pip install onyx-paid-mcp && python -m onyx_paid_mcp.server`
- Source: https://github.com/dimitrilaouanis-tech/onyx-mcp
- Payment protocol: https://x402.org
- Network: Base mainnet (USDC). Base Sepolia for testing. Dual-broadcast manifest (both networks listed per service).

## How agents pay

1. Agent calls a tool. Server replies HTTP 402 with payment requirements (recipient, amount, scheme).
2. Agent signs an EIP-3009 USDC authorization with its wallet, includes it in the next request.
3. Server settles via the CDP x402 facilitator and returns the tool result.

No state is held between calls. No account exists. The wallet IS the identity.

## Tool catalog (67 tools)

### On-chain primitives (Base)
| Tool | What it does | Price |
| --- | --- | --- |
| base_tx_explainer | Decode any Base mainnet tx into a human-readable summary | $0.05 |
| base_tx_simulator | Pre-flight a Base tx — gas estimate + revert detection | $0.10 |
| base_tx_decode | Raw tx ABI decode for Base | $0.002 |
| base_token_risk_scan | Rug-vector scan for any Base ERC-20 (owner, mint, supply, code) | $0.25 |
| token_metadata | ERC-20 name/symbol/decimals/supply on Base | $0.001 |
| ens_resolve | ENS name ↔ address bidirectional resolve | $0.0008 |
| aml_screen | EVM address sanctions + risk-list screen | $0.01 |
| **base_swap_quote** | Best-route swap quote across ~15 Base DEXes via KyberSwap (Uniswap V2/V3, Aerodrome, BaseSwap, Pancake, ...). Returns amountOut + gas + route hops. | $0.002 |
| **base_event_logs** | Fetch + decode contract event logs from Base RPC. Auto-decodes Transfer/Approval/OwnershipTransferred. Topic-0 filter + block range. | $0.003 |
| **base_dex_pair_lookup** | Every DEX pair for a token: price, liquidity USD, 24h volume, price change %. Sorted by liquidity. Returns vol:liq health signal. | $0.0015 |
| **base_bridge_quote** | Cross-chain bridge route from Base to Ethereum/Optimism/Arbitrum/Polygon/etc. Aggregates ~30 bridges via LI.FI. Returns toAmount, fees, gas, ETA. | $0.003 |
| **base_contract_verify** | Contract verification + ABI metadata via Blockscout. Auto-resolves EIP-1967 proxies to implementation. Returns RED/YELLOW/GREEN risk verdict. | $0.002 |
| **agent_audit_trail** | Full payment audit trail for any agent wallet: every USDC outflow, x402-resolved destination, tool name, timestamp, tx hash, cumulative spend, velocity, behavioral risk flags. Catena/Ralio-thesis primitive. | $0.05 |
| **partnership_check** | Where does Onyx plug into Company X's stack? Probes domain + CDP discovery + awesome lists + GitHub for MCP/x402 footprint. Returns gap analysis + outreach subject lines. | $0.02 |
| ⚡ **mcp_router** | **THE meta-router.** Describe a capability ('Base tx explainer'); the router queries the full 1000-route CDP discovery corpus, scores every match by price + network + freshness + schema, returns ranked routes with full call templates. First MCP aggregator. | $0.01 |

### On-chain primitives (Solana)
| Tool | What it does | Price |
| --- | --- | --- |
| solana_tx_explainer | Decode any Solana mainnet tx (half OATP price) | $0.05 |
| solana_token_metadata | SPL name/symbol/decimals/supply via Mint+Metaplex | $0.0008 |
| solana_token_risk_scan | Rug-vector scan for SPL (mint/freeze authority + concentration) | $0.25 |
| solana_jupiter_quote | Best-route swap quote across all Solana DEXes | $0.001 |
| solana_wallet_activity | Recent on-chain activity for any Solana wallet (parsed) | $0.002 |

### Web + utilities
| Tool | What it does | Price |
| --- | --- | --- |
| solve_captcha | OCR-based bypass for image captchas (~30ms) | $0.003 |
| url_text | Fetch a URL and return clean main-content text | $0.001 |
| url_unshorten | Resolve any redirect chain to its final URL | $0.0005 |
| url_parse | URL → structured components | $0.0003 |
| dns_lookup | A / AAAA / PTR records for a host | $0.0005 |
| whois | WHOIS / RDAP registration data for a domain | $0.001 |
| email_validate | Syntax + DNS + role-account + disposable-domain check | $0.0008 |
| ip_geolocate | Country, region, city, ASN, proxy/hosting flags | $0.0008 |
| user_agent_parse | Browser, OS, device, bot/crawler detection | $0.0003 |
| password_strength | Entropy + pattern score for a candidate password | $0.0003 |
| fx_convert | Mid-market FX across 160+ currencies | $0.0008 |
| html_meta | OG / Twitter / JSON-LD extract from any URL | $0.0008 |
| jwt_decode | JWT inspector (header / payload / sig present) | $0.0003 |
| hash_compute | md5 / sha1 / sha256 / sha512 / sha3 | $0.0003 |
| robots_check | robots.txt fetch + path-allowed verdict | $0.0005 |

### Browser (CDP automation)
| Tool | What it does | Price |
| --- | --- | --- |
| browser_navigate / screenshot / extract / click / type / eval | Full CDP browser suite | $0.002–$0.008 |
| agent_workflow | Compose up to 10 tools in one paid call (atomic) | $0.020 |

### MCP + x402 ecosystem ops
| Tool | What it does | Price |
| --- | --- | --- |
| **mcp_meta_call** | Universal x402 preflight inspector — chain picker + agent ID + price gate composed | $0.001 |
| **mcp_oauth_audit** | Grade any MCP server on OAuth 2.1 + DCR compliance (RFC 7591/8414/9728) | free |
| **mcp_catalog_diff** | Side-by-side catalog diff across paid MCP servers | free |
| **x402_indexer_health** | Probe 6 indexers (CDP, Bazaar, awesome-x402, awesome-mcp, x402scan, Smithery) for any URL — diagnose your own discoverability gap | free |
| **x402_chain_picker** | Multi-factor chain ranker — live gas + facilitator presence + finality | $0.0005 |
| **x402_receipt_verify** | Verify a USDC settlement tx on Base/Sepolia, multi-RPC fallback | $0.001 |
| **x402_error_explain** | 14-pattern plain-English HTTP error explainer for x402 flows | free |
| **x402_demo_wallet** | Generate a throwaway test wallet on Base Sepolia for sandbox calls | free |
| **x402_spec_lookup** | Quote the x402 spec by section number / topic — docs-as-tool | free |
| **agent_id** | Wallet-address → derived agent identity + RPC log-scan profile | $0.001 |
| **agent_budget_tracker** | Wallet spend/revenue breakdown across x402 endpoints | $0.0008 |
| **facilitator_health** | Parallel probe of 5 known facilitators — who's alive this week | free |
| **bazaar_submit** | Submit a URL to Coinbase Bazaar discovery | free |
| **bazaar_compare** | Side-by-side comparison across the x402 ecosystem catalog | free |

### Research intel (the new HYPER ANGLE for autonomous agents)
| Tool | What it does | Price |
| --- | --- | --- |
| **research_intel** | Has someone solved X already? Query OpenAlex (240M+ works incl. arXiv preprints), ranked by citations + recency, returns top N with abstract excerpts + synthesis. Saves hyper-agents from re-deriving known results. | $0.05 |
| **paper_synthesis** | Given 2-10 OpenAlex/DOI IDs, structural synthesis: shared keyword signal, citation co-graph, convergence verdict (converging/contested/fragmented). Composes after research_intel. No LLM tokens consumed. | $0.03 |
| **bazaar_blue_ocean** | Find empty x402 market niches. Reads CDP discovery (1000+ services), surfaces categories with 0-1 services. For paid-MCP builders deciding what to ship next. | $0.01 |

## Why this exists

Most MCP servers expect free unlimited use. Real services cost money. x402 lets an MCP server bill per call without forcing the agent to open an account or hold an API key. Onyx Actions is the reference implementation of that pattern — every tool is real, every call settles on-chain, every agent pays the same price.

## Author

Onyx Council — https://github.com/dimitrilaouanis-tech/onyx-mcp

## Discovery surfaces

- agentic.market: indexed automatically on first settled mainnet payment
- x402 directories: x402-list.com · x402list.fun · x402.direct · signal402.com · x402daily.xyz
- awesome-x402: https://github.com/xpaysh/awesome-x402/pull/295 (open)
- Smithery: https://smithery.ai/servers/dimitrilaouanis/onyx-mcp
- Coinbase Bazaar / CDP discovery: indexed on next crawl (dual-broadcast manifest)
- Official MCP Registry: io.github.dimitrilaouanis-tech/onyx-mcp

## Build your own paid MCP

The `onyx-paid-mcp` Python framework that runs Onyx Actions is published on PyPI. Five lines stand up your own paid server. See the GitHub README for the full pattern.

## Contact

- GitHub Issues: https://github.com/dimitrilaouanis-tech/onyx-mcp/issues
- Repo discussions: https://github.com/dimitrilaouanis-tech/onyx-mcp/discussions
